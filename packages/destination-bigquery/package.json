{
  "name": "@onepipe/destination-bigquery",
  "type": "module",
  "version": "0.0.1",
  "description": "OnePipe BigQuery destination plugin for data pipeline integration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*",
    "setup.js"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build",
    "setup": "node setup.js"
  },
  "dependencies": {
    "@maccman/web-auth-library": "catalog:",
    "lodash": "catalog:",
    "@onepipe/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:dev"
  }
}
