{
  "name": "OnePipe Analytics",
  "description": "Open-source Segment alternative built on Cloudflare Workers",
  "tags": ["analytics", "segment", "bigquery", "cloudflare"],
  "repository": {
    "url": "https://github.com/YOUR_USERNAME/onepipe-template"
  },
  "env": {
    "GOOGLE_OAUTH_CLIENT_ID": {
      "description": "Google OAuth Client ID for BigQuery authentication",
      "value": "",
      "required": false
    },
    "GOOGLE_OAUTH_CLIENT_SECRET": {
      "description": "Google OAuth Client Secret for BigQuery authentication", 
      "value": "",
      "required": false
    }
  },
  "build": {
    "command": "npm install && npm run build",
    "cwd": "/",
    "watchPaths": [
      "src/**/*",
      "packages/**/*"
    ]
  },
  "deploy": {
    "command": "node template/deploy.js",
    "cwd": "/"
  },
  "formation": {
    "web": 1
  },
  "success_url": "/setup",
  "postdeploy": "echo 'Visit your worker URL /setup with the setup token to complete configuration'"
}